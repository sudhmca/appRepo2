apiVersion: v1
kind: Service
metadata:
  name: demo-svc
  namespace: gitops-demo
spec:
  selector:
    app: demo-app
  ports:
    - name: http
      port: 80
      targetPort: 3003
      protocol: TCP
  type: ClusterIP